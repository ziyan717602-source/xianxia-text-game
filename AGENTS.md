# 文字修仙项目 Agent 指南

本仓库用于开发一款受 `A Dark Room` 启发的纯文字/低图形化修仙增量叙事游戏。目标是借鉴“渐进揭示、资源循环、探索解锁、叙事反转”的结构，不复刻任何现有游戏、小说、影视或仙侠 IP 的名称、人物、剧情、地图、宗门设定。

## 产品原则

- 先做可玩的核心循环，再扩展世界观。每个新增系统都必须强化“修行、瓶颈、选择、代价”。
- 基调是淡然旁观，不煽情、不评判。世界按规则运转，玩家自择其路，系统只记录后果。
- 开局普通，不做“天崩开局”或饥荒求生。玩家可以从寻常身份入道，修仙本身就是漫长压力。
- 文本是第一界面。UI 可以现代，但不能用营销页、堆卡片或过度插画替代实际玩法。
- 玩家逐步解锁吐纳、采药、炼丹、坊市、宗门、秘境、历劫、飞升等层级。
- 寿元是一等压力，不只是叙事词。时间推进、闭关、失败和飞升结算都要能读到寿元。
- 五行、灵根、功法相性、丹药药性和地点灵气是修仙味的底层语法；早期可以隐藏复杂度，但数据模型要预留。
- 所有玩法都要能被数据解释：资源、速率、条件、风险、冷却、事件权重、存档迁移。
- 保持原创命名。可以使用通用仙侠概念，如灵气、筑基、金丹、元婴、丹药、法器、阵法，但不要直接搬用《凡人修仙传》《仙剑奇侠传》等作品的专有设定。
- 起名可以直白、朴素、有功能感，如“小吐纳法”“稳息诀”“养炉法”，避免堆砌空泛华丽词。
- 不要早早锁定职业或道途。道途应由玩家长期行为、资源偏好和错过的选择自然长出来；有些选择会一直存在，有些选择会来不及。
- 世界要显得鲜活但不围着玩家转：季节、物价、坊市、散修、同门、仇怨、人情、机缘都可以自行变化。
- 文字选择必须写入状态账本。重要选择至少留下 flag/tag/quality，后续可被事件、价格、NPC、劫数或日志读取。
- 第一版也要有轻量地点模型和关系账本：地点影响行动/事件，关系记录债、人情、仇、同门、交易和旧识。
- 斗法占比可以低，但不能完全缺席；优先做成风险事件和资源检验，而不是纯攻击数值或动作战斗。
- 重复积累必须有升格路径：手动操作用于理解系统，之后应逐步转为日课、闭关、阵法、雇佣、洞府或宗门供给。

## 默认技术方向

- 首选浏览器可运行的本地 Web 游戏，方便分享、托管和跨设备开发。
- 玩法状态应拆分为纯逻辑层和 UI 层：`src/game` 放模拟、资源、事件、存档，`src/ui` 放界面。
- 数值与世界内容尽量数据驱动，优先放在 `src/content` 或 `data`，避免把剧情和数值散落在组件里。
- 存档必须有版本号；任何破坏性存档变更都要写迁移逻辑。
- 随机事件应支持固定 seed 或可注入随机源，便于测试。
- 默认技术栈为 Vite + TypeScript + React，核心游戏逻辑保持框架无关。详见 `docs/technical-direction.md`。

## 当前实现状态

- 阶段 1C 第一阶段可玩链路已完成：普通开局、引气入体、炼气一层、日课、世界日志和早期手感基准已存在。
- 阶段 2 已启动：普通出身、灵根五行、功法相性、炼丹丹毒第一版、稳息散稳冲路径和炼气突破第一版已接入。
- 当前优先级是筑基大瓶颈、清躁/解毒事件、宗门影子和重复积累升格，详见 `PLAN.md` 和 `docs/project-status.md`。
- 每次切换开发机后先运行 `npm ci`；当前工作机如遇 WindowsApps `node.exe` 权限问题，先将 `C:\Program Files\nodejs` 前置到 PATH。

## 开发流程

- 改动前先阅读 `docs/game-design.md`、`docs/experience.md` 和本文件。
- 设计改动前还要阅读 `docs/design-direction.md`；实现改动前阅读 `docs/project-status.md` 和 `PLAN.md`。
- 内容、事件、地点、灵根、丹药或宗门相关改动前，还要阅读 `docs/content-bank/README.md` 和对应素材文件。
- 每次引入新玩法，补充或更新对应设计记录，尤其是资源循环、失败条件、解锁条件。
- 重要经验沉淀到 `docs/experience.md`，包括 AI 工具提示词、测试方法、踩坑和可复用脚本。
- 外部资料沉淀到 `docs/research/`，记录来源链接、可借鉴点和不可直接复用的版权边界。
- 如果使用 AI 代理继续开发，先让代理生成短计划，再动代码；完成后要求它说明测试结果。

## 代码质量

- 核心游戏逻辑要有单元测试：资源 tick、境界突破、事件触发、存档读写、解锁条件。
- UI 行动列表必须通过地点可用行动和解锁状态共同过滤，不能只读 `unlockedActions`。
- UI 资源面板必须遵循渐进揭示，不能开局展示所有资源。
- tick 离线结算不能长期依赖逐 tick while 循环，涉及离线收益时要做批量结算。
- UI 改动要做至少一次浏览器烟测；当前可运行 `npm run smoke` 做基础 Edge/CDP 烟测，复杂交互再使用 Playwright 或 Codex in-app browser。
- 数值变更要说明目的：延长节奏、减少等待、提高决策密度、降低惩罚等。
- 内容素材要先机制化再落地：每条事件至少明确地点/季节/条件/权重/后果，不把长设定直接塞进 UI。
- 不引入大框架或复杂服务端，除非它能明显降低长期维护成本。
- 禁止把 API key、私有 token、登录态、个人路径写进仓库。

## Git 与协作

- `main` 只放可运行状态；实验玩法用分支。
- 提交粒度按“一个可解释的产品变化”组织，不把资料、配置、无关重构混在同一提交。
- 云端远程仓库配置见 `docs/git-cloud.md`。
- GitHub Copilot/Cursor/Claude Code 等工具的项目指令应引用本文件，保持多工具一致。
