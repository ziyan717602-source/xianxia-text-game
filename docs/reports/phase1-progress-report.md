# 文字修仙项目：阶段 1 (最小可玩原型) 开发进度报告

## 1. 已完成的工作总结 (Step 1 ~ Step 12)

在连续的开发过程中，我们成功地为游戏打下了完整的底层逻辑与 UI 框架。目前 `main` 分支已具备完整的可玩闭环。

### 核心功能层 (`src/game/`)
- **状态与类型 (`types.ts`, `state.ts`)**：定义了 `GameState` 及其包含的资源（真气、体力、寿元、见闻等）、修仙特有属性（境界、灵根）和时间模型。
- **Tick 引擎 (`tick.ts`)**：实现了按真实时间（1秒 = 1 Tick）流逝的核心引擎，处理体力恢复与寿元衰减，支持离线/挂机增量的逻辑扩展。
- **行为与解锁 (`actions.ts`, `unlock.ts`)**：实现了基于条件的行动判定系统和渐进揭示（Progressive Unlock）机制，玩家的行动会自动增加特性标签（Qualities）用于后续事件。
- **事件与危机 (`events.ts`, `combat.ts`)**：实现了基于权重的随机事件引擎，并集成了小型的判定机制（如战斗、交涉、避让），提供奖励或惩罚。
- **账本系统 (`choices.ts`, `relationships.ts`)**：实现了持久化的选项 Flag 系统、Tag 标记和基础的人物关系状态存储。

### 表现与存储层 (`src/ui/`, `src/storage/`)
- **UI 骨架 (`App.tsx`)**：构建了分栏界面的 React 前端，左侧展示状态，中间展示行动，右侧滚动输出仙途日志。
- **事件弹窗**：实现了能打断主循环的“机缘/变故”沉浸式交互遮罩。
- **主循环 Hook (`useGameLoop.ts`)**：连接了前端渲染与纯逻辑层的 Tick 运转。
- **版本化存档 (`save.ts`)**：接入了 `localStorage` 自动存取，并设计了 `migrateSaveData` 函数防止后续迭代破坏旧存档。

---

## 2. 遇到的问题及修复过程

在开发和进行第 12 步（Playwright/浏览器子代理自动化烟测）时，遇到并解决了一些隐性问题，并根据审核反馈进行了深度重构：

1. **“开局死局”的逻辑漏洞与叙事重构**
   - **问题**：原计划中，解锁“吐纳”需要积攒属性触发事件。为了防卡死临时放入了“读书(dushu)”和“休息(xiuxi)”，但“读书”等词汇在仙侠题材中非常违和，且初始状态不够抓人。
   - **修复与重构**：将开局行动重构为极简的 `['kuzuo']`（枯坐）。玩家在枯坐中获得“神识”，进而触发全新设计的开局事件“发现残破玉简”（探查玉简刺痛手指，获得第一丝真气），从而自然解锁“吐纳”。
2. **仙侠名词的全面本地化 (Thematic Consistency)**
   - **重构**：执行了全代码库的替换，将违和的属性重命名：`knowledge -> insight` (神识/悟性)、`stamina -> essence` (精元)。前端 UI 对应更新为“精元”、“神识”、“枯坐”、“调息”等修仙专属词汇。
3. **事件引擎的 UI 断层**
   - **问题**：设计案中要求选择事件选项（Event Choice），但在原本纯挂机的 UI 骨架中没有交互入口。
   - **修复**：在 `GameState` 引入了 `activeEventId`，当事件触发时暂停 Tick 时间流动，并在前端弹出强制交互的选项面板，选择后发放收益并恢复游戏运转。
4. **TypeScript 严格模式的编译冲突**
   - **问题**：引入 Vite 模板时，内置启用了 `erasableSyntaxOnly`，这导致传统的 `enum` 写法在编译时报错。
   - **修复**：调整了 `tsconfig.json` 配置，关闭了部分影响快速迭代的纯类型严格限制，保证了构建顺畅。

---

## 3. 尚未完成的工作 (Step 13 ~ Step 15)

剩余工作主要聚焦于**内容填充**与**手感调优**：

- **[ ] Step 13：构建第一阶段的完整事件链**
  - 需要在 `content/events.ts` 和 `content/actions.ts` 中丰富事件与行动。
  - 实现一条跑通“凡人 -> 引气入体 -> 炼气一层”的完整叙事和资源逻辑链。
- **[ ] Step 14：季节流转与状态影响**
  - 引入 `Season` 流转对精元恢复、草药产出、或坊市物价的全局 Modifiers 影响机制。
- **[ ] Step 15：体验测试与数值初调**
  - 打包当前版本，试玩半小时，校准动作带来的精元消耗速度与自然恢复速度是否平衡。
  - 优化离线时间计算逻辑（将 `tick.ts` 中的 `while` 循环优化为公式化批量结算，防止离线过久导致的浏览器卡死）。

---

## 4. 后续开发注意事项 ⚠️

1. **存档向下兼容**：目前 `CURRENT_SAVE_VERSION = 1`。在后续迭代中，如果向 `GameState` 添加了任何新的必填属性，**必须**在 `save.ts` 的 `migrateSaveData` 中为其写入默认值。
2. **双机开发的一致性**：项目已经锁定了 `.npmrc`（`lockfile-version=3`）。在工作电脑和个人 PC 之间切换时，请记得**每次拉取代码后使用 `npm ci`**。
3. **UI 逻辑剥离**：不要把任何数值逻辑写在 `App.tsx` 中。所有对状态的改变都必须通过调用 `src/game/` 里的引擎函数实现，以保证未来可以进行 Headless 测试。
4. **A Dark Room 渐进揭示机制**：后续 UI 开发需贯彻此机制，即“资源未获得前不显示”（如：未获得草药前，面板上不应存在草药这一行）。
